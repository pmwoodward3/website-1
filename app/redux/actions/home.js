export const expandSection = (payload) => ({
  type: 'EXPAND_SECTION_HOME',
  payload,
})

export const closeSection = (payload) => ({
  type: 'CLOSE_SECTION_HOME',
  payload,
})

export const setSectionRowLength = (payload) => ({
  type: 'SET_SECTION_ROW_LENGTH_HOME',
  payload,
})
